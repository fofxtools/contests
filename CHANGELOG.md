# Changelog

## [1.0.8] - 2026-09-03

### Added

- Canonical entrant registry (`public/lib/entrants.php`): one id per entrant,
  with spelling variants folded to a canonical name via `entrant_canon()`.
  Generated by `scripts/gen-entrants.php`, validated by
  `scripts/build-entrants.php`, reviewable at `data/entrants.md`.
- Append-only id ledger (`data/entrant-ids.json`) so entrant ids never renumber.
- `type` (character / game / series / rivalry / year) on every contest in the
  canonical match list.

### Changed

- All Match Results (`/node/100`): the "Name aliases" list now comes from the
  entrant registry — 24 alias groups, up from 7.

### Fixed

- Garbled entrant names, corrected in the `matches` and `updates` database
  tables: HTML entities (`&quot;`, `&amp;`, `&#039;`), a Unicode ellipsis, and
  stray leading whitespace.
- Removed a duplicate poll-snapshot row from `updates` (matchnum 1624).
