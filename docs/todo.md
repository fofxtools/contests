# TODO

Short backlog. "Where was I" session notes live in `tmp/`, not here.

- **Game-pool aliases.** Game titles map one string to one id, so the same game
  can show up as several entrants (e.g. Ocarina of Time ×2, Wind Waker ×4).
  Deferred until game rankings matter — see `tmp/entrant-analysis/DECISIONS.md`.
